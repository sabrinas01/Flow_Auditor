# 📑 ESPECIFICACIÓN DE REQUISITOS DE SOFTWARE (SRS)
## Proyecto: Notion Flow Auditor
**Módulo:** Auditoría de Productividad, Recordatorios Diarios y Recordatorios Varios  
**Versión:** 4.12 (Recordatorios Varios: agrupar por fecha de creación, solo pendientes)  
**Estado:** Validado para Desarrollo y QA  
**Autor:** IT Functional Analyst (Sabrina)  
**Fecha de Emisión:** Julio 2026  

---

## 🛠️ HISTORIAL DE CONTROL DE VERSIONES

| Versión | Fecha | Autor | Descripción del Cambio / Hito Técnico |
| :--- | :--- | :--- | :--- |
| **v1.0** | Junio 2026 | Sabrina Sanso | MVP local inicial: script monolítico en primer plano con bucle `while True` bloqueante (`time.sleep`). |
| **v2.0** | Junio 2026 | Sabrina Sanso | Migración CI/CD Serverless en contenedores virtuales GitHub Actions y normalización binaria de BOM Windows. |
| **v3.3** | Julio 2026 | Sabrina Sanso | **Baseline Oficial Sincronizada con PRD v3.3:**<br>- Modificación de frecuencia de sincronización invariable a 1 hora (60 minutos).<br>- Delegación estricta del cómputo matemático de métricas y tasas al cliente JavaScript en tiempo real.<br>- Incorporación de la jerarquía de 6 estados ordenada desde "Sin empezar" hasta "Fallida / Vencida".<br>- Adición del Módulo Semanal de Bienestar y Alerta Emocional con persistencia local en caché (`localStorage`).<br>- Reconfiguración del footer de diagnóstico técnico relativo y posicionamiento del campo de versión del release abajo a la izquierda. |
| **v3.4** | Agosto 2026 | Sabrina Sanso | **Gobernanza de Documentación Versionada:**<br>- Adición de `SRS-FR-M2-205`: gate de CI (`docs_sync_check.yml`) que bloquea PRs con cambios funcionales sin actualización correspondiente de PRD/SRS.<br>- Formalización en `CLAUDE.md` del flujo de sincronización de PRD, SRS e Historias de Usuario (Notion) ante cada cambio funcional. |
| **v3.5** | Agosto 2026 | Sabrina Sanso | **Implementación de Sección IV (Mañana):**<br>- `SRS-FR-M3-303` cerraba solo parcialmente: el backend calculaba e inyectaba `conteoManana` pero el frontend nunca lo renderizaba (solo Ayer y Hoy). Se agregó el tercer bloque "Vista previa de mañana" en `index.html`, reutilizando `renderizarFilasEstados()`.<br>- Alcance deliberadamente reducido respecto al resto de la estructura tripartita: sin métricas de completadas/consistencia (no aplican a tareas futuras) y sin colapsar/expandir (pendiente, ver HU en Notion). |
| **v3.6** | Agosto 2026 | Sabrina Sanso | **Corrección de alcance — Mañana sale de Recordatorios Diarios:**<br>- Se revierte el bloque "Mañana" agregado en v3.5: `index.html` ("Recordatorios Diarios") vuelve a estructura bipartita (Ayer/Hoy).<br>- `SRS-FR-M3-303` se reescribe: la Sección "Tareas para mañana" pasa a ser alcance del futuro módulo **Recordatorios Varios** (HU ya creada en Notion por Sabrina), no de Recordatorios Diarios.<br>- El backend (`extract_and_audit.py`) sigue calculando `conteoManana` sin cambios — queda disponible para cuando se implemente Recordatorios Varios. |
| **v3.7** | Agosto 2026 | Sabrina Sanso | **Colapsar/expandir + cobertura de tests HTTP:**<br>- `SRS-FR-M3-303` cierra del todo: las cards de Ayer y Hoy ahora colapsan/expanden de forma independiente (botón con chevron rotativo, `toggleBloque()` en `index.html`), como ya describía el requisito ("bloques dinámicos colapsables").<br>- `SRS-FR-M1-105` gana cobertura de test real: `tests/test_extract_and_audit_http.py` mockea `requests.post` para probar conexión exitosa (escribe la plantilla) y fallos HTTP 401/500 (`sys.exit(1)`, no toca el archivo), sin llamadas de red ni escritura sobre el `index.html` real (usa `BASE_DIR` redirigido a un directorio temporal). |
| **v3.8** | Agosto 2026 | Sabrina Sanso | **Normalización de BOM en .env:**<br>- `SRS-FR-M1-104` implementado: `cargar_env_local()` en `extract_and_audit.py` lee el `.env` en binario, detecta firmas BOM de UTF-8-SIG y UTF-16 LE/BE, imprime `[ADVERTENCIA] BOM detectado en .env — normalizado automáticamente` y delega el parseo final a `python-dotenv` vía stream ya normalizado.<br>- 4 tests nuevos en `tests/test_env_bom.py` cubren: sin BOM, UTF-8-SIG, UTF-16 LE y UTF-16 BE. |
| **v3.9** | Agosto 2026 | Sabrina Sanso | **Utilitarios huérfanos resueltos:**<br>- `SearchBar.js` (componente React) eliminado: dependía de React + bundler, incompatible con el stack estático vigente (HTML5/Tailwind/JS vanilla, sin build step).<br>- `debounce.js` y `src_localStorage_index.js` reescritos sin `import`/`export` (con export CommonJS opcional para tests) y conectados de verdad a `index.html` vía `<script>`: el botón de refresco ahora usa el `debounce()` compartido en vez de un `setTimeout` inline, e `initLocalStorage()` corre en cada carga de página inicializando/migrando el estado versionado (`flow_auditor:v1.0`).<br>- Se corrigió el `require()` roto de `tests/unit/localStorage.test.js` (apuntaba a un archivo que nunca existió); sigue sin poder correr en CI porque el repo no tiene Jest/npm instalado — pendiente si se decide invertir en tooling de test JS. |
| **v4.0** | Agosto 2026 | Sabrina Sanso | **Versión de producción sincronizada:**<br>- Producción mostraba `v1.0.0` (único tag de git existente) mientras PRD/SRS ya iban por v3.9 — desincronización de 4 fuentes distintas de "versión" (tag de git, footer hardcodeado `v5.4`, docstring de `extract_and_audit.py` `Versión 5.4`, y PRD/SRS).<br>- Se eliminan los números de versión hardcodeados en `index.html` (placeholder `v5.4`→`dev`) y en el docstring de `extract_and_audit.py` — la única fuente de verdad pasa a ser el tag de git (`SRS-FR-M2-204`).<br>- Se documenta en `CLAUDE.md` la política: tagear un release (`git tag -a vX.Y`) cuando un bump de PRD/SRS sea un cambio de producto visible para el usuario, no cada bump de gobernanza de documentación.<br>- Se crea el tag `v4.0` sobre este commit. |
| **v4.1** | Agosto 2026 | Sabrina Sanso | **Performance y accesibilidad (motivado por el check `lighthouse` en rojo):**<br>- Accesibilidad 88→100: corregido el contraste del botón "Ir a recordatorios diarios" (`#ff5f52`→`#c0453b` de fondo) y del texto de copyright (`#5a5a5e`→`#8e8e93`, reutilizando un color ya validado en la página); se quita `user-scalable=no`/`maximum-scale` del viewport (bloqueaba el zoom, violación de accesibilidad móvil).<br>- Performance 37→68: se reemplaza `cdn.tailwindcss.com` (127KB de JS + compilación JIT en cada carga, ~1.7s de bootup-time) por un CSS estático precompilado (`styles.css`, generado desde `tailwind.config.js` + `src/styles/input.css` vía `npm run build:css`); se acota el rango de variación solicitado a Google Fonts para Material Symbols (`wght` de 100..700 a 400 fijo), reduciendo esa fuente de 1.1MB a 458KB; se comprime `grafica/logo 1-1 NF.png` de 1024×1024/933KB a 160×160/49KB (se mostraba a 36-40px en pantalla).<br>- `package.json`/`package-lock.json` nuevos: primera dependencia de build (Node, solo para regenerar `styles.css`; el runtime del sitio sigue siendo HTML/CSS/JS estático sin bundler). |
| **v4.2** | Agosto 2026 | Sabrina Sanso | **Cobertura de tests JS (Jest):**<br>- Se instala Jest + jsdom (`npm test`). Hasta esta versión el repo no tenía ningún test de JS corriendo — `tests/unit/localStorage.test.js` apuntaba a un archivo inexistente.<br>- Se extrae la lógica de cálculo pura de `index.html` (motor de consistencia, umbral 70%, generación de los últimos 7 días, conteo de días bajos para la Alerta de Bienestar `SRS-FR-M3-306`) a `src/utils/dashboard_logic.js`, separada de la manipulación de DOM, siguiendo el mismo patrón vanilla+CommonJS que `debounce.js`.<br>- **Bug real encontrado por los tests nuevos:** `debounce.js` no reiniciaba el temporizador en cada llamada — dos invocaciones separadas por más del delay podían disparar la función dos veces en vez de colapsar en una. Corregido.<br>- 34 tests nuevos: `dashboard_logic.test.js` (escapeHtml, esEstadoCompletado, obtenerEstiloEstado, calcularMetricas, generarUltimos7Dias incl. bordes de mes/año, calcularResumenSemanal), `debounce.test.js` (fake timers), más el test de `localStorage` ya corrigiendo su ruta. |
| **v4.3** | Agosto 2026 | Sabrina Sanso | **Tests de cron, versión y sincronización (`SRS-FR-M2-202`, `SRS-FR-M2-204`):**<br>- `obtener_version_actual()` y el cálculo de timestamps (antes inline dentro de `auditar_consistencia_tripartita()`, uno de ellos como función anidada) se extraen a nivel de módulo en `extract_and_audit.py` como `obtener_version_actual()` y `calcular_timestamps_sincronizacion()`, sin cambiar su comportamiento.<br>- 6 tests nuevos en `tests/test_sincronizacion.py`: el cron de `notion_sync.yml` se parsea con PyYAML y se verifica que sea `0 * * * *`; `obtener_version_actual()` con `subprocess` mockeado (tag presente y fallback a `"dev"` sin tags); `calcular_timestamps_sincronizacion()` cubre la conversión GMT-3 y el borde de medianoche en ambos sentidos (retrocede de día y cruza al día siguiente).<br>- Con esto la HU "Despliegue Automatizado y Trazabilidad de Versiones" no tiene gaps pendientes. |
| **v4.4** | Agosto 2026 | Sabrina Sanso | **RAM en runtime, jerarquía de estados y tests de renderizado DOM:**<br>- `SRS-NFR-PERF-002`: se agrega `verificar_consumo_ram()` a `extract_and_audit.py` — monitoreo de RAM *dentro* del script (antes solo existía como test externo). Si el RSS supera 50MB imprime `[ALERTA] Consumo RAM > 50MB (actual: XXX MB) — revisando fugas de memoria` sin detener la ejecución. `psutil` pasa de dependencia de test a dependencia de runtime (`requirements.txt`). 4 tests en `tests/test_ram_monitoring.py` con `psutil.Process` mockeado.<br>- **`SRS-FR-M3-308` (nuevo):** la jerarquía de estados del PRD §4.2 nunca estuvo implementada — `renderizarFilasEstados` solo pintaba `Object.entries(data)` en el orden de llegada. Se agrega `rankEstado()`/`ordenarEntradasPorJerarquia()` en `dashboard_logic.js` y se aplica antes de renderizar.<br>- Se extrae `renderizarFilasEstados`/`toggleBloque` (antes inline en `index.html`) a `src/utils/dom_render.js`, testeadas con Jest + jsdom en `tests/unit/dom_render.test.js` (renderizado, orden jerárquico en el DOM, escape de XSS, colapsar/expandir).<br>- 80 tests en total en el repo (28 Python + 52 JS), todos en verde. |
| **v4.5** | Agosto 2026 | Sabrina Sanso | **Test de paridad local/CI (`SRS-FR-M2-201`, `SRS-FR-M2-203`):**<br>- 2 tests nuevos en `tests/test_paridad_local_ci.py`: verifican que la ruta CI (Secrets ya en el entorno) y la ruta local (`.env` cargado vía `cargar_env_local()`) resuelvan exactamente la misma credencial final, tanto con los nombres primarios (`NOTION_API_KEY`) como con los alternativos (`NOTION_TOKEN`/`NOTION_DATABASE_ID`).<br>- Se descarta explícitamente crear `.env.example`: no aporta valor suficiente frente al costo de mantenerlo sincronizado, y las variables ya están documentadas en `Documentacion/configuration.md`. |
| **v4.6** | Agosto 2026 | Sabrina Sanso | **`inicio.html` como página principal del sitio (nuevo `SRS-FR-M3-309`):**<br>- `index.html` (el dashboard) sigue siendo la plantilla maestra que `extract_and_audit.py` lee y reescribe cada hora — no se toca su pipeline ni su path hardcodeado.<br>- Se agrega un script al `<head>` de `index.html` que, si `sessionStorage['nfa_landed']` no existe, lo setea y redirige (`location.replace`) a `inicio.html`. Así la primera visita de la sesión (típicamente por la URL raíz servida vía GitHub Pages, donde `index.html` es el único nombre de archivo que Pages puede servir como raíz) cae en `inicio.html`, sin generar un loop cuando el usuario navega después a `index.html` desde el link "Recordatorios diarios" del sidebar de `inicio.html`.<br>- Se evaluó y se descartó el swap completo de nombres de archivo (mover el dashboard a `dashboard.html` y renombrar `inicio.html`→`index.html`) por el costo de actualizar los paths hardcodeados en `extract_and_audit.py`/`generate_dashboard.py`, tests y workflows — decisión explícita de Sabrina, ver `CLAUDE.md`. |
| **v4.7** | Agosto 2026 | Sabrina Sanso | **Módulo Recordatorios Varios (nuevo `recordatorios-varios.html`, cierra `SRS-FR-M3-303`):**<br>- Se implementa `recordatorios-varios.html` a partir del diseño Stitch aportado por Sabrina, reutilizando el Design System "Obsidian Refined" y los mismos IDs/patrón de card colapsable que `index.html` (Ayer/Hoy), sumando una tercera card "Tareas planificadas" que renderiza `conteoManana` — la sección que `SRS-FR-M3-303` había dejado explícitamente fuera de alcance de Recordatorios Diarios en v3.6 encuentra acá su hogar definitivo. Nuevo requisito `SRS-FR-M3-310`.<br>- `extract_and_audit.py` ahora itera sobre `("index.html", "recordatorios-varios.html")` aplicando el mismo bloque de sustituciones regex a ambos archivos en el mismo ciclo horario, en vez de escribir un único `html_path` hardcodeado (`SRS-FR-M3-301` extendido a ambos frontends).<br>- El sidebar de `index.html`/`inicio.html` habilita el link "Recordatorios varios" (antes `<span>` deshabilitado con `cursor-not-allowed` y title "Próximamente").<br>- `tests/test_extract_and_audit_http.py` extendido: la plantilla mínima temporal ahora se crea para ambos archivos y las 3 aserciones verifican la sincronización/no-mutación en ambos. |
| **v4.8** | Agosto 2026 | Sabrina Sanso | **Fix de despliegue — `recordatorios-varios.html` nunca llegaba a producción (cierra `SRS-FR-M2-206`, nuevo):**<br>- El paso "Publish" de `notion_sync.yml` hacía `git checkout -B gh-pages` + `git add index.html` únicamente. `extract_and_audit.py` sí escribía `recordatorios-varios.html` actualizado en el runner (v4.7 lo agregó al loop de archivos), pero al no estar en el `git add`, ese archivo quedaba como cambio sin stagear y se descartaba al terminar el job — nunca se commiteaba ni pusheaba a `gh-pages`.<br>- Confirmado en el historial real de `gh-pages`: desde que se mergeó v4.7 (`d6608b0`), la corrida automática horaria (`cbc314f`, "auto: actualización horaria...") solo modificó `index.html`; `recordatorios-varios.html` seguía anclado al commit humano original.<br>- Fix: `git add index.html recordatorios-varios.html`. Sin test de regresión automatizado todavía (requeriría simular el step de shell del workflow, no solo el script Python) — queda como gap conocido, ver Sección 5 (Fuera de Alcance de esta versión). |
| **v4.9** | Agosto 2026 | Sabrina Sanso | **Recordatorios Varios conectado a su propia base de Notion (nuevo `SRS-FR-M1-106`):**<br>- Hasta esta versión `recordatorios-varios.html` mostraba exactamente los mismos números que `index.html`: `extract_and_audit.py` consultaba una sola base (`DB_RECORDATORIOS_DIARIOS`) y escribía el mismo resultado en los dos frontends. La variable `NOTION_DB_RECORDATORIOS_VARIOS` existía en el `.env` local de Sabrina pero el código nunca la leía, y el secret no estaba configurado en GitHub Actions.<br>- Se agrega `DB_RECORDATORIOS_VARIOS` como credencial requerida (sin fallback alternativo, a diferencia de Diarios) validada en `validar_credenciales()`.<br>- La lógica de consulta+clasificación (antes inline en `auditar_consistencia_tripartita()`) se extrae a `consultar_y_clasificar(database_id, etiqueta_log)`, reutilizada dos veces: una por cada base. Cada frontend recibe los `conteoAyer`/`conteoHoy`/`conteoManana` de su propia base — dejan de compartir datos.<br>- `notion_sync.yml` mapea el nuevo secret a las variables de entorno del step "Run Notion Flow Auditor" y lo valida en "Verify Secrets".<br>- 2 tests nuevos: `test_diarios_y_varios_son_bases_independientes` (verifica que se consulta cada base por su propio ID y que los dos frontends terminan con conteos distintos) y `test_ruta_local_y_ruta_ci_resuelven_la_misma_db_de_varios` (paridad local/CI para la nueva variable, mismo patrón que `SRS-FR-M2-201`/`203`). |
| **v4.10** | Agosto 2026 | Sabrina Sanso | **Aislamiento de fallos + lista individual en Recordatorios Varios (HU "Conectar e implementar bd recordatorios varios", cierra `SRS-FR-M4-401` a `404`):**<br>- **Bug corregido:** desde v4.9, `DB_RECORDATORIOS_VARIOS` era una credencial requerida y se consultaba en el mismo bloque `try` que Recordatorios Diarios — un 401/500/timeout en esa base (o directamente no tenerla configurada) hacía `sys.exit(1)` y abortaba **todo** el pipeline, incluyendo `index.html`. `SRS-FR-M1-105` decía explícitamente "si una falla, no debe escribirse ningún frontend", lo cual iba en contra del criterio de aceptación original de la HU (fallo aislado y no fatal).<br>- `DB_RECORDATORIOS_VARIOS` pasa a ser **opcional** (`required=False`). `auditar_consistencia_tripartita()` ahora sincroniza Recordatorios Diarios primero (fatal, sin cambios de comportamiento ahí) y recién después, en un bloque `try/except` aislado, sincroniza Recordatorios Varios — cualquier error ahí (BD no configurada, HTTP 401/500, timeout, I/O) se loguea y se omite sin propagar.<br>- **Cambio de modelo de datos:** `recordatorios-varios.html` deja de reutilizar `conteoAyer`/`conteoHoy`/`conteoManana` (conteos agregados, sin mostrar Nombre/Prioridad/Área/Periodo de cada ítem) y pasa a `recordatoriosVarios`, un array plano con un objeto por recordatorio (`nombre`, `estado`, `prioridad`, `area`, `periodo`, `fecha`) — **sin filtrar por fecha**, mostrando el universo completo de la BD. Nueva función `extraer_recordatorios_varios_lista()` en el backend; nueva función `renderizarRecordatoriosVarios()` en el frontend (reutiliza `escapeHtml`/`obtenerEstiloEstado` de `dashboard_logic.js`).<br>- `SRS-FR-M1-105`, `SRS-FR-M1-106` y `SRS-FR-M3-310` reescritos; `SRS-FR-M4-401` a `404` (nuevos) documentan el módulo D dedicado a Recordatorios Varios.<br>- `tests/test_extract_and_audit_http.py` reescrito: separa los casos fatales (401/500 en Diarios → `sys.exit(1)`, ningún archivo se toca) de los aislados (401/500/no-configurada en Varios → Diarios se sincroniza igual, Varios queda con lista vacía, sin `sys.exit`). `styles.css` regenerado (`npm run build:css`) para incluir las clases Tailwind nuevas del listado (`min-w-0`, `whitespace-nowrap`, etc.). |
| **v4.11** | Agosto 2026 | Sabrina Sanso | **Fix: Recordatorios Varios filtra por Ayer/Hoy/Mañana (reportado por Sabrina: "sigue mostrando mal los datos de hoy"):**<br>- **Bug corregido:** v4.10 traía *todos* los ítems de la BD "Mis Recordatorios varios V0" en una sola lista sin filtrar por fecha — mezclaba recordatorios de ayer, hoy y mañana (y cualquier otro día) indiscriminadamente.<br>- `extraer_recordatorios_varios_lista()` se reemplaza por `extraer_recordatorios_varios_clasificados()`: clasifica cada ítem con `evaluar_bloque_temporal()` (la misma función que usa `consultar_y_clasificar()` para Recordatorios Diarios) y devuelve tres listas (`items_ayer`, `items_hoy`, `items_manana`) — cada una sigue siendo una lista de ítems completos (Nombre/Estado/Prioridad/Área/Periodo/Fecha), no un conteo agregado.<br>- `sincronizar_recordatorios_varios()` inyecta tres constantes (`recordatoriosVariosAyer`/`Hoy`/`Manana`) en vez de una sola `recordatoriosVarios`.<br>- `recordatorios-varios.html` pasa de una card única a **tres cards colapsables** (Ayer/Hoy/Mañana), reutilizando `toggleBloque()` de `src/utils/dom_render.js` (mismo patrón visual que `index.html`). `renderizarRecordatoriosVarios()` se parametriza por prefijo (`list-${prefijo}`, `total-${prefijo}-lbl`) para renderizar cada bloque.<br>- `SRS-FR-M4-402`/`403`/`404` reescritos; `tests/test_extract_and_audit_http.py` agrega `test_recordatorios_varios_clasifica_por_bloque_cronologico` (ítems con fecha de ayer/hoy/mañana terminan cada uno en su bloque correcto) y actualiza el resto de las aserciones a las tres constantes nuevas. |
| **v4.12** *(Actual)* | Agosto 2026 | Sabrina Sanso | **Recordatorios Varios: agrupar por fecha de creación, solo pendientes (pedido explícito de Sabrina):**<br>- La clasificación Ayer/Hoy/Mañana de `extraer_recordatorios_varios_clasificados()` pasa a usar `pagina.get("created_time")` (fecha de creación de la página en Notion) en vez de la propiedad `Fecha` — que sigue extrayéndose y mostrándose como metadato de cada ítem, ya sin usarse para clasificar.<br>- Nueva función `_es_estado_pendiente(estado)`: excluye ítems en estado terminal ("Hecha", "Hecha por otra persona", "⏭️ No necesaria", "❌ Fallida / Vencida" — coincidencia case-insensitive por substring, igual criterio que `esEstadoCompletado()`/`obtenerEstiloEstado()` en `dashboard_logic.js`). Se aplica antes de clasificar por bloque cronológico, así que un ítem terminal no cuenta ni siquiera para descartar el bloque.<br>- `recordatorios-varios.html` no requiere cambios: sigue recibiendo tres listas ya filtradas y clasificadas, agnóstico del criterio usado para construirlas.<br>- `SRS-FR-M4-402` reescrito; `tests/test_extract_and_audit_http.py` reemplaza `test_recordatorios_varios_clasifica_por_bloque_cronologico` por `test_recordatorios_varios_clasifica_por_fecha_de_creacion` (un ítem con `Fecha` de mañana pero creado hoy cae en el bloque Hoy) y agrega `test_recordatorios_varios_filtra_estados_terminales`. |

---

## 1. INTRODUCCIÓN

### 1.1 Propósito
Esta Especificación de Requisitos de Software (SRS) define de forma rigurosa y formal los requerimientos de ingeniería para el sistema **Notion Flow Auditor** en su **versión 3.3**. Este documento actúa como la Única Fuente de Verdad (*Single Source of Truth - SSOT*) técnica y operativa, traduciendo las directivas de negocio, diseño y experiencia del usuario del PRD en especificaciones de software verificables y testeables por desarrollo y QA.

### 1.2 Ámbito del Sistema (Scope)
El sistema es una herramienta híbrida y asíncrona de procesamiento de flujos de trabajo de productividad personal. Su función principal consiste en extraer de forma segura datos de una base de datos privada de Notion, estructurar arrays planos JSON en el backend, inyectarlos mediante expresiones regulares en una plantilla estática y delegar la lógica de cálculo métrico, histórico semanal y renderizado adaptivo al cliente web expuesto de forma segura en entornos móviles.

**Componentes del Software:**
* `extract_and_audit.py`: Script extractor backend escrito en Python 3.10 (ejecución headless local o Actions runner).
* `index.html`: Frontend de "Recordatorios Diarios" (Ayer/Hoy + Balance Semanal), construido con Tailwind CSS y Vanilla JavaScript (ECMAScript 6).
* `recordatorios-varios.html`: Frontend de "Recordatorios Varios" — clasificado en las mismas tres ventanas cronológicas que Recordatorios Diarios (`recordatoriosVariosAyer`/`Hoy`/`Manana`), pero cada bloque es una lista de ítems individuales, no un conteo agregado. Mismo stack y Design System que `index.html`.
* `notion_sync.yml`: Orquestador de Integración y Despliegue Continuo (CI/CD) de GitHub Actions.

### 1.3 Definiciones, Acrónimos y Abreviaturas
* **NFA:** Notion Flow Auditor.
* **ART:** Argentina Time (GMT-3), huso horario de referencia de la analista en San Juan, Argentina.
* **UTC:** Coordinated Universal Time, huso horario utilizado por los servidores y marcas de transacciones cloud.
* **BOM (Byte Order Mark):** Firma binaria en cabeceras de archivos de texto que genera excepciones en compilaciones multiplataforma.
* **SSOT (Single Source of Truth):** Práctica de gobernanza orientada a unificar la lógica del producto en un repositorio/documento central.

### 1.4 Referencias
* Estándar IEEE 830-1998 / ISO/IEC/IEEE 29148 (Ingeniería de Requisitos).
* Product Requirements Document (PRD) v3.3 — Notion Flow Auditor (NFA).
* Manual de Identidad Visual Corporativa Premium (Brand Book Pomelli) — Bitácora IT.

---

## 2. DESCRIPCIÓN GENERAL

### 2.1 Perspectiva del Producto
**Notion Flow Auditor** opera como un sistema de sincronización desacoplado y reactivo de solo lectura. El backend en Python funciona como un cliente asíncrono que extrae la información de Notion en entornos aislados (locales o serverless) y delega el cómputo matemático de métricas y porcentajes en tiempo real al navegador del cliente final, protegiendo los tokens de seguridad de accesos públicos y optimizando la velocidad de carga móvil.

### 2.2 Características de los Usuarios
* **Analista de Productividad (Sabrina Sanso):** Perfil técnico funcional que requiere un acceso inmediato, fluido y mobile-first a sus métricas de consistencia diaria sin lidiar con terminales de comandos interrumpiendo su entorno Windows.

---

## 3. REQUISITOS ESPECÍFICOS

### 3.1 Requisitos Funcionales (FR)

#### Módulo A: Backend, Extracción e Inteligencia de Datos (Python)
* **SRS-FR-M1-101 (Conectividad Segura HTTPS):** El script debe inicializar un socket seguro utilizando TLS 1.3 mediante bloques de contexto `with requests.Session()` para la destrucción inmediata de sockets tras la consulta a los endpoints de la API de Notion.
* **SRS-FR-M1-102 (Clasificación Temporal Algebraica):** El sistema debe calcular algebraicamente mediante objetos nativos `datetime.date` las tres ventanas temporales exactas: Ayer (`hoy - 1`), Hoy (`fecha actual`), y Mañana (`hoy + 1`).
* **SRS-FR-M1-103 (Estructuración y Serialización de Datos):** El backend debe recuperar el universo de registros de Notion, filtrar los ítems bajo la condición lógica `estado == "Hecha" AND consistencia == 1`, y serializar los arrays de datos planos limpios (`conteoAyer`, `conteoHoy`, `conteoManana`) al cliente en formato JSON plano, absteniéndose de precargar totales fijos en el script.
* **SRS-FR-M1-104 (Normalización de Codificación Windows):** El módulo de lectura local debe realizar un análisis binario crudo del archivo `.env` para detectar e interceptar firmas BOM, forzando la sanitización de esquemas complejos (`UTF-16 LE`, `UTF-8-SIG`) generados por PowerShell o entornos Windows.
* **SRS-FR-M1-105 (Gobernanza de Errores Estricta — Recordatorios Diarios):** Ante cualquier fallo de credenciales (HTTP 401), errores de DNS o caídas del servidor de Notion (HTTP 5xx) al consultar `DB_RECORDATORIOS_DIARIOS`, el script backend debe capturar de forma controlada la excepción, imprimir un log descriptivo y finalizar con el código de salida estricto `sys.exit(1)` sin escribir `index.html`. Esto aplica **únicamente** a Recordatorios Diarios — un fallo al consultar Recordatorios Varios sigue un régimen distinto y no fatal, ver `SRS-FR-M4-401`.
* **SRS-FR-M1-106 (Bases de Notion Independientes por Módulo):** Recordatorios Diarios y Recordatorios Varios deben consultarse contra bases de datos de Notion distintas (`NOTION_DB_RECORDATORIOS_DIARIOS`, requerida, y `NOTION_DB_RECORDATORIOS_VARIOS`, opcional — ver `SRS-FR-M4-401`), sin fallback cruzado entre sí. Ambas bases se clasifican por las mismas tres ventanas cronológicas (Ayer/Hoy/Mañana, `evaluar_bloque_temporal`), pero cada una con su propio modelo de datos por bloque (Recordatorios Diarios: `conteoAyer`/`conteoHoy`/`conteoManana` agregados por estado; Recordatorios Varios: lista de ítems completos por bloque, `SRS-FR-M4-402`) e inyectando cada resultado únicamente en su frontend correspondiente (`index.html` para Diarios, `recordatorios-varios.html` para Varios) — nunca duplicar el resultado de una base en el otro frontend.

#### Módulo B: Orquestación, Pipeline y CD (GitHub Actions)
* **SRS-FR-M2-201 (Detección Condicional de Runtime):** El backend debe evaluar al inicio de la ejecución si la variable `os.getenv("GITHUB_ACTIONS") == "true"`. En caso positivo, desactivará el bucle infinito local para integrarse al ciclo serverless de una única ejecución.
* **SRS-FR-M2-202 (Orquestación del Ciclo de 1 Hora):** El pipeline YAML de sincronización automatizada debe autoinvocarse de forma periódica invariable cada 1 hora (60 minutos) utilizando la sintaxis cron: `0 * * * *`.
* **SRS-FR-M2-203 (Redundancia y Robustez de Secrets):** El flujo YAML implementará compuertas lógicas condicionales (`||`) en su bloque de entorno para capturar secretos ante variaciones comunes de nomenclatura web (mapear de forma segura tanto `NOTION_API_KEY` como `NOTION_TOKEN`).
* **SRS-FR-M2-204 (Trazabilidad de la Versión del Release):** El pipeline de Actions capturará de forma automática el número de tag activo en el repositorio Git e inyectará dicho string en el identificador de pie de página de la interfaz (Ej: `v3.3`).
* **SRS-FR-M2-206 (Publicación Completa de Frontends):** El paso de publicación de `notion_sync.yml` debe agregar (`git add`) y commitear a `gh-pages` **todos** los archivos frontend que `extract_and_audit.py` actualiza en esa corrida (`index.html` y `recordatorios-varios.html`), no un subconjunto fijo. Cualquier frontend nuevo que se sume al loop de sincronización del backend debe agregarse también a este paso en el mismo cambio, para evitar que quede con datos congelados en producción pese a estar deployado.
* **SRS-FR-M2-205 (Gobernanza de Documentación Versionada):** Todo Pull Request contra `main` que modifique archivos funcionales (`extract_and_audit.py`, `generate_dashboard.py`, `index.html`, `src/**` o `notion_sync.yml`) debe fallar el workflow `docs_sync_check.yml` si `Documentacion/PRD.md` y `Documentacion/SRS.md` no fueron actualizados en el mismo PR, salvo que un commit incluya la marca explícita `[skip-docs]`. Este control garantiza que el PRD, el SRS y las Historias de Usuario en Notion permanezcan versionados en sincronía con cada cambio funcional (ver `CLAUDE.md` en la raíz del repo).

#### Módulo C: Frontend e Interfaz Móvil (HTML5/Tailwind/JS)
* **SRS-FR-M3-301 (Inyección mediante Regex Tolerantes):** El parser de Python debe actualizar dinámicamente las constantes de JavaScript utilizando expresiones regulares flexibilizadas (`re.sub()`) con comodines de captura tolerantes (`\s*`) para la mutación estática segura de strings. Las constantes de telemetría (`timestampLocalStr`, `timestampNextStr`, `timestampServerStr`, `appVersionStr`) se inyectan con el mismo bloque de sustituciones en `index.html` y `recordatorios-varios.html`; el resto de las constantes (`conteoAyer`/`conteoHoy`/`conteoManana` vs. `recordatoriosVarios`) es específico de cada frontend, según su propio modelo de datos (`SRS-FR-M1-106`).
* **SRS-FR-M3-302 (Cómputo en Tiempo Real del Cliente):** El motor JavaScript del cliente en el navegador móvil debe calcular dinámicamente en tiempo real los totales de tareas y las Tasas de Consistencia (%) sobre los arrays JSON planos inyectados, aplicando la fórmula: `(Tareas con Estado "Hecha" AND consistencia == 1 / Total de Tareas Creadas en el Día) * 100`.
* **SRS-FR-M3-303 (Estructura Cronológica Bipartita — Recordatorios Diarios):** La interfaz móvil adaptará su visualización mediante columnas o bloques dinámicos colapsables de Tailwind para renderizar por separado las secciones: I (Tareas de ayer) y II (Tareas de hoy). La Sección "Tareas para mañana" queda **fuera de alcance de este módulo**: `index.html` no debe renderizar `conteoManana` (el backend lo sigue calculando e inyectando, sin uso en el frontend). Recordatorios Varios (Módulo D) sí clasifica por las tres ventanas (Ayer/Hoy/Mañana, `SRS-FR-M4-402`), pero con un modelo de datos distinto: cada bloque es una lista de ítems completos, no un conteo agregado por estado.
* **SRS-FR-M3-304 (Sincronización de Diagnóstico Técnico Relativo):** El panel de diagnóstico técnico del footer expondrá en tipografía secundaria *Inter* las siguientes variables dinámicas:
    1. *Última Sincronización Local:* Fecha y hora exacta convertida a la zona ART (GMT-3).
    2. *Próxima Sincronización (Dynamic ART):* Marca de tiempo calculada dinámicamente sumando estrictamente 1 hora (60 minutos) a la hora de la última sincronización real realizada, prohibiendo horas redondeadas fijas.
    3. *Volumen de Tareas Procesadas:* Total bruto de ítems procesados descargados de la API.
* **SRS-FR-M3-305 (Registro de Cabecera en Formato UTC Fijo):** En la zona superior de la interfaz, inmediatamente al lado del botón interactivo de refresco animado, se renderizará de forma fija la hora de la transacción en formato UTC del servidor: `YYYY-MM-DD HH:mm:ss UTC`.
* **SRS-FR-M3-306 (Módulo Semanal de Bienestar y Alerta Emocional):** El frontend mantendrá un historial en la caché local del navegador (`localStorage`) de las tasas diarias de los últimos 7 días. Si la tasa de consistencia cae por debajo del 70.0% en 4 o más días de la semana actual, se inyectará dinámicamente en el DOM la tarjeta de alerta con el mensaje empático literal: *"Linda, es momento de ajustar prioridades y analizar si son necesarias tantas tareas. No seas dura contigo, cielo. 🧠✨"*.
* **SRS-FR-M3-307 (Identificador de Release y Versión):** La interfaz de usuario debe renderizar el string de la versión inyectada en un campo posicionado abajo a la izquierda, alineado exactamente debajo de la base del botón de acción de navegación que redirige a Notion (`Ir a recordatorios diarios`).
* **SRS-FR-M3-308 (Jerarquía de Estados en el Renderizado):** Al renderizar la lista de tareas de un bloque (Ayer/Hoy), la interfaz debe agrupar y ordenar las filas respetando estrictamente la jerarquía definida en el PRD §4.2: 1. Sin empezar, 2. En ejecución, 3. Hecha por otra persona, 4. No necesaria, 5. Hecha, 6. Fallida / Vencida — independientemente del orden en que el backend haya serializado los estados. Un estado que no matchee ninguna categoría se renderiza al final, sin romper el resto del orden.
* **SRS-FR-M3-309 (`inicio.html` como Página Principal):** `inicio.html` es la página principal del sitio. Al cargar `index.html` (el dashboard), si `sessionStorage` no tiene la marca `nfa_landed` para la sesión de navegador actual, la interfaz debe setearla y redirigir de inmediato (`location.replace`) a `inicio.html`, sin alterar la plantilla que `extract_and_audit.py` reescribe cada hora. Una vez seteada la marca, cargas subsecuentes de `index.html` dentro de la misma sesión (por ejemplo, navegando desde el link "Recordatorios diarios" del sidebar de `inicio.html`) no deben volver a redirigir.
* **SRS-FR-M3-310 (Navegación a Recordatorios Varios):** El sidebar de navegación de `index.html`/`inicio.html`/`recordatorios-varios.html` debe enlazar al ítem "Recordatorios varios" hacia `recordatorios-varios.html`. Ese frontend no debe incluir el Módulo Semanal de Bienestar (`SRS-FR-M3-306`) ni la tabla de Balance Semanal, exclusivos de Recordatorios Diarios — su estructura de contenido (tres bloques cronológicos con lista de ítems individuales) se especifica en el Módulo D (`SRS-FR-M4-404`).

#### Módulo D: Recordatorios Varios (`recordatorios-varios.html`)
* **SRS-FR-M4-401 (Extracción Aislada y No Fatal):** El backend debe sincronizar Recordatorios Varios como un paso **posterior** a la sincronización exitosa de Recordatorios Diarios, envuelto en su propio `try/except` que nunca invoca `sys.exit()`. Ante la ausencia de `NOTION_DB_RECORDATORIOS_VARIOS` (variable opcional, sin fallback), o ante cualquier fallo HTTP (401/5xx), timeout, o error de E/S al leer/escribir `recordatorios-varios.html`, el sistema debe loguear un mensaje descriptivo y continuar — sin afectar ni revertir la sincronización ya completada de `index.html`.
* **SRS-FR-M4-402 (Filtro de Pendientes, Clasificación por Fecha de Creación y Normalización de Propiedades):** El backend debe descartar primero todo ítem en estado terminal (`_es_estado_pendiente()` devuelve `False` para "Hecha", "Hecha por otra persona", "⏭️ No necesaria" y "❌ Fallida / Vencida" — coincidencia case-insensitive por substring, mismo criterio que `esEstadoCompletado()`/`obtenerEstiloEstado()` en `dashboard_logic.js`); solo los ítems pendientes (Sin empezar, Pospuesta, En ejecución, En espera) se procesan. Cada ítem pendiente debe clasificarse en una de las tres ventanas cronológicas (Ayer/Hoy/Mañana) usando `evaluar_bloque_temporal()` sobre su **fecha de creación** en Notion (`created_time`) — no sobre su propiedad `Fecha` (que es la fecha de vencimiento/programación y solo se usa como metadato, no para clasificar). Un ítem cuya fecha de creación no cae en ninguna de las tres ventanas se descarta. Por cada ítem clasificado, el backend debe normalizar las propiedades de Notion `Nombre` (title), `Estado` (status), `Prioridad` (select), `Área` (select), `Periodo` (select) y `Fecha` (date) a un objeto JSON plano con claves `nombre`, `estado`, `prioridad`, `area`, `periodo` y `fecha`, aplicando valores por defecto ("Sin nombre" / "Sin estado") cuando la propiedad no esté presente en la página. El resultado son tres listas (Ayer/Hoy/Mañana) de solo ítems pendientes, cada una de ítems completos — nunca un conteo agregado por estado.
* **SRS-FR-M4-403 (Inyección en Página Independiente):** El backend debe serializar cada una de las tres listas clasificadas como `const recordatoriosVariosAyer/Hoy/Manana = [...]` (JSON, con escape de `"</"` para prevenir inyección de HTML/XSS) e inyectarlas mediante `re.sub()` en `recordatorios-varios.html`, junto con las mismas marcas de tiempo (`timestampLocalStr`, `timestampNextStr`, `timestampServerStr`) y `appVersionStr` que usa `index.html` (`SRS-FR-M3-301`).
* **SRS-FR-M4-404 (Renderizado en Tres Bloques Colapsables y Codificación Visual por Estado):** El frontend de `recordatorios-varios.html` debe renderizar tres cards colapsables (Ayer/Hoy/Mañana, mismo patrón visual y `toggleBloque()` de `src/utils/dom_render.js` que Recordatorios Diarios). Cada card renderiza una lista (una fila por ítem) con Nombre, Estado, Prioridad, Área, Periodo y Fecha, escapando todo texto interpolado (`escapeHtml()`, reutilizada de `dashboard_logic.js`) para prevenir XSS. Cada fila debe resaltarse visualmente según su Estado reutilizando `obtenerEstiloEstado()` de `dashboard_logic.js` (la misma codificación visual que Recordatorios Diarios): pill roja para "❌ Fallida / Vencida", pill verde para "Hecha", pill azul (default) para el resto.

---

### 3.2 Requisitos No Funcionales (NFR)

* **SRS-NFR-SEC-001 (Criptografía y Privacidad de Datos):** Queda estrictamente prohibido que cualquier token de autenticación (`ntn_...`) o clave de acceso viaje hacia el navegador móvil o quede expuesto en el código plano estático de `index.html`. El archivo `.env` debe estar listado obligatoriamente en el `.gitignore` y las credenciales se inyectarán exclusivamente vía **GitHub Secrets**.
* **SRS-NFR-PERF-002 (Eficiencia de Recursos del Runtime):** El Daemon local en segundo plano ejecutado en Windows debe operar de forma invisible (headless, ocultando la consola CMD) consumiendo menos del 2% de CPU y un máximo de 50 MB de memoria RAM.
* **SRS-NFR-USE-003 (Diseño de Identidad Visual de Alto Contraste Pomelli):** Los componentes visuales y widgets dinámicos del dashboard deben apegarse estrictamente a la paleta industrial del manual de marca: Negro azabache (`#08080A`), Obsidiana Black (`#111115`), acentos en Rojo Toscano (`#B91C1C`) / Rojo suave (`#EF4444`).
* **SRS-NFR-USE-004 (Frecuencia Invariable e Impacto Métrico):** Toda sincronización de infraestructura local o cloud se ejecutará invariablemente cada 1 hora (60 minutos) de forma automatizada. Si el cómputo dinámico de hoy cae por debajo del umbral del 70.0%, la tarjeta del KPI principal cambia su acento visual de forma automática a color Clementine Orange (`#F59E0B`).
* **SRS-NFR-PERF-005 (Rendimiento Web Móvil):** El dashboard estático alojado en **GitHub Pages** debe cargarse en menos de 1.5 segundos en conexiones móviles 4G/5G, manteniendo la interfaz optimizada para pantallas verticales de teléfonos celulares mediante rejillas colapsables de Tailwind.

---

## 4. CRITERIOS DE ACEPTACIÓN CORE (ENFOQUE BDD)

### SRS-BDD-001: Automatización del Ciclo de 1 Hora y Alerta de Bienestar
* **Dado que** el Daemon invisible de Windows o el Actions container finalizó una sincronización exitosa a las `14:00 (ART / GMT-3)`.
* **Y** el sistema ha calculado en su caché de `localStorage` semanal que el usuario acumuló 4 días con tasas de rendimiento inferiores al umbral recomendado del 70.0%.
* **Cuando** el reloj del sistema llega a las `15:00 (ART / GMT-3)`.
* **Entonces** la infraestructura automatizada debe despertar, procesar los datos de Notion y publicar el `index.html` en GitHub Pages cumpliendo con:
    1. Calcular de forma relativa la Próxima Actualización y renderizar en el footer exactamente: `16:00 ART` (Última Sincronización Local + 1 hora).
    2. Habilitar en la sección III de "Balance Semanal" la tabla dinámica con el historial y desplegar de forma visible la caja con el mensaje de bienestar literal: *"Linda, es momento de ajustar prioridades y analizar si son necesarias tantas tareas. No seas dura contigo, cielo. 🧠✨"*.
    3. Mostrar en la zona superior de la interfaz, de forma fija al lado del botón de refresco animado, la hora exacta en formato UTC del servidor: `YYYY-MM-DD 18:00:00 UTC`.
    4. Mostrar abajo a la izquierda, exactamente debajo de la base del botón del footer, la etiqueta de versión de release `v3.3`.
    5. Mapear y agrupar el panel de distribución cuantitativo de la sección de hoy respetando el orden jerárquico estricto: *1. Sin empezar, 2. En ejecución, 3. Hecha por otra persona, 4. No necesaria, 5. Hecha, 6. Fallida / Vencida*, aplicando el color *Clementine Orange (#F59E0B)* en el KPI de hoy por estar por debajo del umbral mínimo de consistencia.